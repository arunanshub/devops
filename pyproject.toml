[project]
name = "arunanshu-infrastructure"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.17",
    "kubernetes>=29.0",
]

[dependency-groups]
dev = [
    "ansible-lint>=25.0",
]
