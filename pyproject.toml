[project]
name = "arunanshu-infrastructure"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "ansible-core>=2.21.1",
    "kubernetes>=36.0.2",
]

[dependency-groups]
dev = [
    "ansible-lint>=26.6.0",
]
