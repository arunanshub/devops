# Auth: token-based login (temporary). Long-term: GitHub/Google OIDC via k3s
# --oidc-issuer-url + --oidc-client-id. See memory: headlamp-auth-decision.md
replicaCount: 1

config:
  inCluster: true
  inClusterContextName: "main"

ingress:
  enabled: false

resources:
  requests:
    cpu: 25m
    memory: 64Mi
  limits:
    memory: 256Mi
