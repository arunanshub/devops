*.tfstate
*.tfstate.backup
kubeconfig.yaml
*.retry
.ansible/
__pycache__/
 
# OpenTofu working directory
.terraform/
 
# Ignore SSH known hosts file to prevent conflicts with existing entries.
.ssh_known_hosts

devbox.d/

# Implementation plans — local development aid, not for version control
docs/plans/
